# 项目技术文档

## 1. 项目概述

本项目是一个前后端分离的在线学习管理平台，包含以下核心业务模块：

- 登录认证
- 动态 RBAC 权限管理
- 工作台统计与图表展示
- 课程管理
- 学生管理
- 学习总结 Markdown 展示
- 前端路由守卫与登录态持久化
- 后端静态托管前端打包产物

从项目体量上看，它不是一个超大型系统，但它很好地覆盖了中后台项目中最常见、也最适合面试展开讨论的能力点：

- 前后端分离架构
- 统一接口封装
- 登录鉴权、动态权限配置与接口权限边界
- 状态管理
- 列表页增删改查
- 表单校验
- 图表可视化
- 前后端接口契约维护
- MySQL + TypeORM 数据建模
- 服务端数据聚合
- Markdown 渲染与静态资源访问

如果把它当成一个学习项目，它的价值不只是“能跑”，更在于它把一个完整管理后台从页面、状态、接口、数据库到部署串起来了。

---

## 2. 技术栈总览

### 2.1 前端

- React 19
- TypeScript
- Vite
- React Router DOM
- Zustand
- Axios
- Ant Design
- ECharts
- Tailwind CSS

### 2.2 后端

- Node.js
- NestJS
- TypeScript
- @nestjs/typeorm
- TypeORM
- mysql2
- jsonwebtoken
- bcryptjs

`server` 目录仍保留 Koa + SQLite 旧版后端，用于功能对照、迁移参考和临时回退；默认开发与部署以后以 `server-nest` 为主。

### 2.3 数据层

- MySQL 8
- TypeORM Entity
- TypeORM Migration
- 首次启动初始化默认角色、权限和账号
- 近 7 天学习活跃度由数据层每日补齐

### 2.4 构建与运行

- 前端开发环境通过 Vite 启动
- 后端通过 NestJS 启动
- 前端开发时通过 Vite 代理 `/api` 到 NestJS
- 生产或演示环境由 NestJS 提供 REST API 和静态资源访问能力

---

## 3. 项目目录结构

```text
course/
├─ client/                  # React 前端
│  ├─ src/
│  │  ├─ components/        # 通用组件与图表组件
│  │  ├─ layouts/           # 应用布局
│  │  ├─ pages/             # 页面级组件
│  │  ├─ router/            # 路由与守卫
│  │  ├─ stores/            # Zustand 状态管理
│  │  ├─ style/             # 全局样式与 Markdown 样式
│  │  ├─ utils/             # 请求、文本、分页等工具
│  │  ├─ api.ts             # 统一 API 封装
│  │  ├─ auth.ts            # 登录态持久化
│  │  ├─ permissions.ts     # 前端权限码镜像与权限判断工具
│  │  ├─ markdown.tsx       # 轻量 Markdown 渲染器
│  │  ├─ types.ts           # TypeScript 类型定义
│  │  ├─ App.tsx            # 主题与路由挂载
│  │  └─ main.tsx           # 前端入口
│  └─ package.json
├─ server/                  # Koa + SQLite 旧版后端，作为迁移对照与回退参考
│  ├─ data/                 # SQLite 数据库与 Markdown/静态资源
│  ├─ src/
│  │  ├─ database/          # 数据库连接与初始化
│  │  ├─ middleware/        # 中间件
│  │  ├─ routes/            # 各业务路由
│  │  ├─ services/          # 权限服务层
│  │  ├─ utils/             # 响应工具
│  │  ├─ permissions.js     # 后端权限码字典、默认角色和权限分组
│  │  └─ index.js           # 服务入口
│  └─ package.json
├─ server-nest/             # 默认 NestJS 后端
│  ├─ src/
│  │  ├─ auth/              # 登录、/auth/me、密码修改
│  │  ├─ common/            # 统一异常、路由 ID 解析等公共工具
│  │  ├─ course-categories/ # 课程分类字典管理接口
│  │  ├─ courses/           # 课程管理接口
│  │  ├─ dashboard/         # 工作台统计接口
│  │  ├─ database/          # TypeORM 配置、实体、migration、初始化任务
│  │  ├─ permissions/       # 权限码、角色权限业务服务
│  │  ├─ students/          # 学生管理接口
│  │  ├─ summary/           # 学习总结 CRUD
│  │  ├─ system/            # 账号、角色、权限管理接口
│  │  ├─ upload/            # 图片上传接口
│  │  └─ static/            # 静态资源访问接口
│  ├─ test/                 # API 契约、初始化、migration、工具函数测试
│  └─ package.json
├─ README.md
└─ 项目技术文档.md
```

---

## 4. 系统整体架构

## 4.1 架构模式

本项目采用典型的前后端分离架构：

1. 前端负责页面渲染、交互、状态管理、表单校验。
2. 后端负责认证、数据查询、数据写入、统计聚合、静态资源读取。
3. 数据库存储业务实体与统计基础数据。
4. 前端通过 HTTP 调用后端统一接口。

可以把整个请求链路理解成：

```text
用户操作
  -> React 页面组件
  -> Zustand store
  -> api.ts
  -> request.ts(Axios 拦截器)
  -> NestJS Controller
  -> NestJS Service
  -> TypeORM Repository / QueryBuilder
  -> MySQL
  -> 返回统一 JSON
  -> store 更新状态
  -> 页面重新渲染
```

---

## 5. 前端架构设计

## 5.1 前端启动流程

入口文件是 `client/src/main.tsx`，主要做两件事：

1. 获取根节点 `#root`
2. 以 `StrictMode` 挂载 `App`

`App.tsx` 负责更高一层的应用装配：

1. 使用 `ConfigProvider` 配置 Ant Design 主题
2. 引入全局样式
3. 挂载 `AppRouterProvider`

这说明作者把“应用主题”和“应用路由”作为最顶层能力集中处理，而不是散落在页面中。

---

## 5.2 路由设计

前端路由定义在 `client/src/router/routes.tsx`。

路由结构如下：

- `/login`：登录页
- `/dashboard`：工作台
- `/courses`：课程管理
- `/students`：学生管理
- `/summary`：学习总结
- `/accounts`：账号管理
- `/permissions`：权限管理
- `/403`：无权限页面

其中 `/` 不是直接渲染页面，而是使用 `ProtectedLayoutElement` 作为受保护布局，再由 `Outlet` 承载子页面。

这是一种很常见也很合理的后台系统设计：

- 登录页与业务页分离
- 业务页共用一个统一布局
- 路由守卫放在布局层，而不是每个页面各写一遍
- 每个业务路由再通过 `RequirePermission` 绑定页面访问权限

---

## 5.3 路由守卫实现

路由守卫分为两层：

- `client/src/router/page-elements.tsx`：处理登录态、布局和页面懒加载。
- `client/src/router/RequirePermission.tsx`：处理页面级权限校验。

### 登录页路由判断

`LoginRouteElement` 的核心逻辑：

- 如果 `authLoading` 为真，说明应用正在校验登录态，此时显示全屏 loading
- 如果已经有 `token` 且 `user` 存在，说明当前用户已登录，直接跳转到 `/dashboard`
- 否则展示登录页

### 受保护布局判断

`ProtectedLayoutElement` 的核心逻辑：

- 初始化阶段先显示 loading
- 如果没有合法登录态，则跳回 `/login`
- 如果已登录，则渲染 `AppShell`

### 为什么这么设计

这是一个面试中经常会被问到的点：

“为什么不用每个页面单独判断是否登录？”

答案是：

- 逻辑集中，避免重复
- 所有业务页面共享同一套判断逻辑
- 更容易维护导航、布局、错误展示
- 更符合 React Router 的嵌套路由设计方式

### 页面级权限守卫

业务路由会通过 `RequirePermission` 包裹，例如：

```tsx
<RequirePermission code={PERMISSIONS.COURSES_VIEW}>
  <CoursesRouteElement />
</RequirePermission>
```

这样即使用户手动输入 `/courses`，也必须拥有 `courses:view` 权限才能进入页面。无权限时会跳转到 `/403`，并携带来源路径和缺失权限码，方便演示和调试。

---

## 5.4 双 Token 登录态与冷启动恢复

前端不再把 Token 或用户信息持久化到 localStorage。`Access Token` 只存在 `client/src/auth.ts` 的模块内存中，生命周期为 15 分钟；`Refresh Token` 由服务端写进 `HttpOnly`、`SameSite=Lax` Cookie，JavaScript 无法读取。

应用冷启动时，`auth-store` 通过 `initializePromise` 合并并发初始化请求，并调用 `POST /api/auth/refresh`。浏览器自动携带 Refresh Cookie，服务端验证 MySQL 会话后返回新的 Access Token 和用户权限；成功则恢复 store，失败则保持未登录状态。进入业务接口时，Axios 从内存取 Access Token 写入 `Authorization: Bearer <token>`。

当接口返回 401，`request.ts` 使用单例 `refreshPromise` 发起一次刷新；并发失败请求等待同一刷新结果并各自重试一次。刷新或重试失败后才清理内存登录态，避免刷新风暴和无限重试。

这种划分兼顾了安全与体验：长生命周期凭据不落到 localStorage，页面刷新仍可无感恢复，而服务端保留撤销会话的能力。

---

## 5.5 前端权限体系设计

前端权限体系围绕服务端下发的 `user.permissions` 展开。

相关文件：

```text
client/src/permissions.ts
client/src/stores/auth-store.ts
client/src/router/nav-config.tsx
client/src/router/RequirePermission.tsx
client/src/components/Permission.tsx
client/src/layouts/AppShell.tsx
```

### 权限码镜像

`client/src/permissions.ts` 维护与后端同名的权限码常量：

```ts
export const PERMISSIONS = {
  COURSES_VIEW: "courses:view",
  COURSES_CREATE: "courses:create",
  COURSES_UPDATE: "courses:update",
  COURSES_DELETE: "courses:delete",
} as const;
```

前端这份权限码不是最终安全来源，而是用于类型提示和 UI 控制。真正的接口权限仍由后端判断。

### auth-store 统一判断权限

`auth-store` 在保存用户信息时也保存 `user.permissions`，并提供：

```ts
hasPermission(code)
hasAnyPermission(codes)
```

业务页面不需要直接操作权限数组，只通过统一方法判断当前用户是否有指定权限。

### 菜单级权限

菜单项集中配置在 `nav-config.tsx`，每个菜单项绑定一个权限码：

```ts
{
  key: "courses",
  path: "/courses",
  label: "课程管理",
  permission: PERMISSIONS.COURSES_VIEW,
}
```

`AppShell` 会根据当前用户权限过滤菜单，没有权限的模块不会出现在侧边栏。

### 路由级权限

业务路由通过 `RequirePermission` 做页面访问控制。菜单隐藏只是体验，路由守卫用来防止用户手动输入 URL 绕过菜单。

### 按钮级权限

页面按钮通过声明式 `Permission` 组件控制：

```tsx
<Permission code={PERMISSIONS.COURSES_CREATE}>
  <Button>新增课程</Button>
</Permission>
```

课程、学生、学习总结、账号管理、权限管理页面都通过这种方式控制新增、编辑、删除、角色分配和权限配置等操作。

---

## 5.6 状态管理设计

前端状态管理使用 Zustand，而不是 Redux。

### 为什么 Zustand 适合这个项目

这个项目的状态规模属于中小型管理后台：

- 登录状态
- 列表页状态
- 表单弹窗状态
- 加载态
- 错误态

它不需要 Redux 那样严格的 action/reducer 模式，因此 Zustand 更轻量、学习成本更低、样板代码更少。

### 各个 store 的职责

#### 1. auth-store

负责：

- 登录
- 登出
- 冷启动登录态校验
- 全局错误信息

#### 2. dashboard-store

负责：

- 工作台数据加载
- 图表与统计数据状态

#### 3. course-store

负责：

- 课程列表数据
- 课程分类辅助数据，来自 `GET /api/course-categories`
- 分页、搜索、筛选、排序
- 弹窗开关
- 新增/编辑/删除/发布

#### 4. student-store

负责：

- 学生列表数据
- 班级和课程支持数据
- 分页、搜索、筛选
- 新增/编辑/删除

#### 5. summary-store

负责：

- Markdown 内容加载

---

## 5.7 Store 设计细节

### 查询状态统一收敛

课程和学生列表都采用了统一的 `query` 对象，包含：

- keyword
- page
- pageSize
- 以及各自模块的筛选条件

好处：

- 列表查询参数集中管理
- 刷新时直接使用当前 query
- 分页、搜索、筛选逻辑统一
- 容易序列化到 URL，方便未来扩展

### draftKeyword 的意义

课程页和学生页都没有直接把输入框值绑定到 query.keyword，而是单独维护 `draftKeyword`。

原因：

- 用户输入过程中不应该每敲一个字就请求接口
- 只有在按回车或点击搜索时，才把草稿值提交到真正查询参数

这是一个很常见的“输入态”和“已提交态”分离设计。

### reset-registry 的意义

`client/src/stores/reset-registry.ts` 用来收集所有 store 的重置函数。

作用：

- 退出登录时清空所有业务状态
- token 失效时同步清理页面状态
- 避免新用户看到旧用户的数据残留

这是一种很实用的全局状态清理方案。

---

## 5.8 页面层设计

页面组件的职责很清晰：

- 读取 store 状态
- 处理交互事件
- 调用 store 方法
- 组织 UI 结构

页面并不直接操作 axios，也不直接处理数据库语义，这说明分层是比较健康的。

### 课程页设计

课程页包含：

- 查询区
- 表格区
- 分页区
- 表单弹窗
- 课程分类管理弹窗

这是典型后台 CRUD 页面的标准形态。课程分类不再从课程表去重得到，而是作为独立字典表维护；课程筛选下拉框和新增/编辑课程表单都会请求数据库中的分类数据。

课程页亮点：

- 选课人数支持服务端排序
- 分类筛选使用分类 UUID 外键，课程表仍保留分类名称快照兜底展示
- 状态切换使用 `Popconfirm`
- 删除后自动计算下一页，避免空页

### 学生页设计

学生页与课程页结构保持高度一致，提升了交互一致性。

学生页亮点：

- 学号失焦异步唯一性校验
- 多选课程通过 `Checkbox.Group`
- 删除按钮只对当前行显示 loading，而不是阻塞整页

### 工作台设计

工作台页面是典型的“统计卡片 + 图表面板”布局。

图表数据不是前端自己拼，而是后端直接返回适合图表消费的结构，这降低了前端数据转换成本。

### 学习总结页设计

总结页的重点在于：

- 从服务端读取 Markdown
- 前端用自写渲染器解析
- 代码块支持高亮与复制

这让项目不仅有传统管理后台能力，还有一定的内容渲染能力。

---

## 5.9 UI 与样式系统

### Ant Design 的作用

Antd 承担了大部分中后台交互组件：

- 表单
- 输入框
- 选择器
- 表格
- 弹窗
- 分页
- 提示反馈

### Tailwind 的作用

Tailwind 用于快速组织布局和局部样式，例如：

- Flex/Grid 排版
- 间距
- 圆角
- 边框
- 背景

### 全局 CSS 的作用

`base.css` 负责统一修正和覆盖 Antd 默认风格，比如：

- 输入框高度
- 边框粗细
- 表格表头视觉
- 按钮风格
- 弹窗风格

说明作者的思路是：

- 组件交互能力交给 Antd
- 局部结构和排版交给 Tailwind
- 整体品牌感通过全局 CSS 收口

这是一种实际项目里很常见的组合拳。

---

## 5.10 图表系统设计

图表相关代码位于 `client/src/components/echarts/`。

### ChartContainer 的作用

`chart-core.tsx` 封装了 ECharts 实例生命周期：

- 初始化图表实例
- 绑定 resize 事件
- 组件卸载时销毁实例
- option 更新时调用 `setOption`

### 为什么要单独封装 ChartContainer

如果每个图表组件都自己写 `echarts.init()`：

- 重复代码会很多
- 生命周期容易写错
- 容易出现内存泄漏

所以封装容器是合理的抽象。

### 为什么使用 notMerge: true

在切换不同数据或配置时，ECharts 默认可能会合并旧配置，导致：

- 旧 series 残留
- legend 错误
- 坐标轴配置冲突

因此这里使用 `notMerge: true` 每次完整覆盖，保证渲染稳定。

### 各图表职责

- `CourseEnrollmentChart`：柱状图，展示课程选课人数排名
- `LearningActivityChart`：折线图，展示最近 7 天学习人数与学习时长
- `StudentStatusChart`：环形图，展示学生状态分布
- `CourseCategoryChart`：环形图，展示课程分类分布

### 图表数据为什么由后端聚合

如果前端自己聚合统计：

- 需要拿更多原始数据
- 网络开销更大
- 前端逻辑更复杂

所以把统计放在后端更符合职责边界。

---

## 5.11 Markdown 渲染器设计

`client/src/markdown.tsx` 是这个项目一个很有意思的亮点。

它没有引入 `marked`、`markdown-it`、`react-markdown` 等库，而是自己实现了一个轻量级 Markdown 解析器。

### 支持的语法

- 标题
- 段落
- 有序列表
- 无序列表
- 代码块
- 图片
- 链接
- 行内代码
- 加粗

### 实现思路

#### 1. 先解析成 Block

代码先把文本转成中间结构：

- heading
- paragraph
- list
- code
- image

再根据 Block 类型渲染为 React 元素。

这是一个非常好的分层：

- 解析逻辑和渲染逻辑分离
- 后续扩展更容易

#### 2. 行内语法用正则扫描

`parseInline()` 使用一个大正则解析：

- `**加粗**`
- `` `代码` ``
- `![图片](url)`
- `[链接](url)`

#### 3. 代码高亮自己实现

通过 `tokenizeLine()` 和 `getHighlightPatterns()` 实现简单高亮：

- 关键字
- 字符串
- 注释
- 数字
- JSX 标签

这不是一个完整的语法高亮器，但对当前项目的展示需求已经足够。

### 这部分可能被面试官追问什么

常见追问包括：

- 为什么不用现成 Markdown 库？
- 自己实现 Markdown 的边界是什么？
- 这种实现有什么风险？

可以这样回答：

- 该项目规模较小，只需要支持有限语法
- 自写实现便于学习原理，也减少依赖
- 缺点是功能有限，不支持复杂嵌套语法，也需要注意 XSS 风险

---

## 6. 后端架构设计

## 6.1 NestJS 服务入口

默认后端入口在 `server-nest/src/main.ts`，应用模块在 `server-nest/src/app.module.ts`。

启动过程：

1. 读取 `.env` 中的 MySQL、JWT、静态资源等配置。
2. 通过 `TypeOrmModule.forRootAsync()` 创建 TypeORM 连接。
3. 在连接前确保目标 MySQL 数据库存在。
4. 按配置执行 TypeORM migration。
5. 注册业务模块，包括 auth、courses、course-categories、students、dashboard、summary、system、upload、static。
6. 启动 `DatabaseInit`，仅补齐系统默认角色、权限、角色权限映射和默认账号。

旧版 `server/src/index.js` 仍保留 Koa 入口，但当前默认开发和测试以 `server-nest` 为准。

### 为什么数据库初始化拆成 migration 和 init

原因是两类职责不同：

- migration 负责表结构，例如 `users`、`courses`、`course_categories`、`students`、`roles`、`permissions`。
- init 负责系统默认数据，例如管理员/教师/学生角色、权限字典、默认账号。
- 近 7 天学习活跃度不是系统权限数据，而是演示统计数据，放在数据层每日补齐。

这样新电脑首次启动可以自动准备基础系统数据，已有数据库也不会被重复覆盖。

---

## 6.2 统一响应结构

后端所有 JSON 响应都使用统一格式：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

失败时：

```json
{
  "code": 400,
  "msg": "错误描述",
  "data": null
}
```

### 这样设计的好处

- 前端拦截器可以统一处理
- 页面层不用每次判断不同接口格式
- 成功与失败结构一致
- 更容易扩展日志和监控

---

## 6.3 鉴权设计

鉴权守卫在 `server-nest/src/auth/auth.guard.ts`，权限守卫在 `server-nest/src/auth/permissions.guard.ts`。

### 鉴权流程

1. 从请求头读取 `Authorization: Bearer <accessToken>`
2. 校验 JWT 签名、过期时间与 `type=access`
3. 校验 JWT 中 `sessionId` 对应的 MySQL 会话仍处于有效状态
4. 成功后把用户信息挂到 NestJS request user 上；任一步失败都返回 401

### 为什么接口鉴权要重新查数据库

JWT 里只能保存登录时的基础用户信息，不能代表当前权限一定仍然有效。

所以权限守卫会根据 `user.id` 重新查询数据库里的当前用户角色，再由 `PermissionService` 计算最新权限。这样管理员调整角色后，旧 token 不会继续拥有过期权限。

---

## 6.4 认证模块设计

认证模块在 `server-nest/src/auth/`。

### 登录、刷新与退出

登录校验用户名密码后，服务端调用 `getEffectivePermissions(user)` 计算权限，创建 `refresh_tokens` 会话记录，并返回 15 分钟有效的 Access Token。随机 Refresh Token 仅以 SHA-256 哈希落库，原始值通过 `course_admin_refresh_token` 的 HttpOnly Cookie 下发。

`POST /api/auth/refresh` 在数据库事务中锁定旧会话、校验 24 小时闲置期和 7 天最大会话期，撤销旧 Token 后创建新 Token，实现轮换。如果已撤销 Token 被再次使用，则撤销其 familyId 下所有会话，防止重放。

`POST /api/auth/logout` 撤销当前会话；`POST /api/auth/logout-all` 和修改密码会撤销用户全部会话。因为 `JwtAuthGuard` 每次都验证会话状态，旧 Access Token 即使还未到期也不能继续访问。

### 为什么登录失败统一提示

项目对“用户名不存在”和“密码错误”都返回：

`用户名或密码错误`

原因：

- 避免暴露用户名是否存在
- 降低被枚举账号的风险

这是一个典型安全设计点。

### `/me` 接口的作用

`/me` 用于在已有 Access Token 时获取当前用户与最新权限；页面冷启动的续期入口是 `/refresh`，而不是从 localStorage 读取 Token。服务端会话状态与权限实时查询共同保证退出、改密和角色调整后不会继续沿用过期登录态。

---

## 6.5 动态 RBAC 后端设计

权限后端主要由四部分组成：

```text
server-nest/src/permissions/permissions.constants.ts
server-nest/src/permissions/permission.service.ts
server-nest/src/auth/permissions.guard.ts
server-nest/src/system/system.controller.ts
```

### 权限码字典

`server-nest/src/permissions/permissions.constants.ts` 是后端权限码的权威来源，维护：

- `PERMISSIONS`：系统支持的权限码，例如 `courses:view`、`students:update`。
- `DEFAULT_ROLES`：默认角色，包括 `admin`、`teacher`、`student`。
- `DEFAULT_ROLE_PERMISSIONS`：默认角色初始权限。
- `PERMISSION_GROUPS`：权限管理页展示用的分组。
- `PERMISSION_DEPENDENCIES`：写操作对查看权限的依赖。

### 角色权限落库

数据库中通过三张表支持动态权限配置：

- `roles`：角色信息，包含内置角色和自定义角色。
- `permissions`：权限字典。
- `role_permissions`：角色与权限的多对多关系。

管理员在权限管理页勾选权限后，后端会把结果保存到 `role_permissions`，后续登录或刷新登录态时再动态计算权限集合。

### 权限服务层

`server-nest/src/permissions/permission.service.ts` 负责权限业务规则：

- `getEffectivePermissions(user)`：根据用户角色计算最终权限。
- `createRole()`：创建自定义角色。
- `updateRolePermissions()`：更新角色权限。
- `normalizePermissions()`：过滤非法权限并补齐依赖权限。
- `listRoles()`：返回角色、权限数量、用户数量、是否可编辑等信息。
- `deleteRole()`：删除未绑定用户的自定义角色。

其中 `admin` 是不可变角色，始终通过 `Object.values(PERMISSIONS)` 获取全部权限，不依赖数据库中的 `role_permissions`。这样即使数据库中的角色权限关系被误删，管理员也不会失去系统管理能力。

### 接口级权限中间件

NestJS 中通过 `JwtAuthGuard` 和 `PermissionsGuard` 组合完成接口级权限控制。

接口鉴权流程：

```text
读取 Authorization: Bearer <token>
  -> 校验 JWT
  -> 根据 token 中的 user.id 重新查询数据库中的当前用户
  -> 根据当前 role 计算权限集合
  -> 判断是否包含接口需要的 permission
  -> 无权限返回 403
```

这里的重点是：接口鉴权不会完全信任 JWT 里携带的旧角色，而是重新查询数据库中的最新角色，避免管理员修改角色后，旧 token 继续越权访问接口。

### 系统管理接口

`server-nest/src/system/system.controller.ts` 提供账号、角色和权限字典接口：

- `GET /api/system/users`：查看账号列表。
- `POST /api/system/users`：新增普通账号。
- `PATCH /api/system/users/:id/role`：修改账号角色。
- `DELETE /api/system/users/:id`：删除普通账号。
- `GET /api/system/roles`：获取角色列表。
- `POST /api/system/roles`：创建自定义角色。
- `PATCH /api/system/roles/:code`：修改角色名称和说明。
- `DELETE /api/system/roles/:code`：删除自定义角色。
- `GET /api/system/permissions`：获取权限分组。
- `PATCH /api/system/roles/:code/permissions`：更新角色权限。

角色管理类接口除了要求 `accounts:updateRole` 权限外，还要求当前用户必须是 `admin`，防止普通角色误拿到账号权限后进一步维护角色体系。

---

## 6.6 课程模块设计

课程接口在 `server-nest/src/courses/`。

### 支持的能力

- 列表查询
- 分类查询
- 单项详情
- 新增
- 编辑
- 删除
- 状态切换

### 列表查询实现

列表接口支持：

- keyword 搜索课程名或讲师
- status 筛选
- category 筛选
- sortField + sortOrder 排序
- page + pageSize 分页

### 查询设计亮点

#### 1. 参数化查询

使用 TypeORM QueryBuilder 的命名参数：

- 灵活
- 安全
- 避免 SQL 注入

#### 2. 排序白名单

排序字段只能从这些值里选：

- student_count
- lesson_count
- created_at
- name

这是为了防止用户把任意字符串拼进 `ORDER BY`。

### 新增与编辑设计

编辑接口使用“未传值则回退 existing”的方式：

- 支持部分更新
- 保证未修改字段不会丢失

### 状态切换设计

课程状态只有两种：

- published
- draft

因此后端直接做二元切换，逻辑简单清晰。

---

## 6.7 学生模块设计

学生接口在 `server-nest/src/students/`。

这是整个后端里最值得仔细学习的模块之一，因为它包含：

- 查询
- 校验
- 关系字段处理
- 冗余统计回写

### 查询能力

支持：

- keyword 搜姓名或学号
- className 筛选
- status 筛选
- courseId 按课程过滤
- 分页

### course_ids 的存储方式

学生和课程是多对多关系，但项目没有单独建中间表，而是直接在 `students.course_ids` 中存 JSON 数组。

例如：

```json
[1, 3, 5]
```

### 这种设计的优点

- 数据结构简单
- 开发速度快
- 对作业项目足够

### 这种设计的缺点

- SQL 查询关系不方便
- 无法高效 join
- 按课程过滤时需要先查再内存过滤
- 扩展性不如中间表

### 为什么仍然可以接受

因为这个项目规模较小，主要目的是学习和演示，权衡开发成本后这是合理选择。

这类“工程取舍”很适合面试聊。

### normalizeStudentPayload 的作用

这是学生服务的核心函数之一，它把新增和编辑的字段校验统一到了一个地方。

它负责：

- 文本清洗
- 回退旧值
- course_ids 归一化
- 学号格式校验
- 手机号格式校验
- 邮箱格式校验
- 状态合法性校验
- 至少选择一门课程校验

### 为什么把校验集中在这里

因为这样可以让：

- POST 和 PUT 共用同一套规则
- 逻辑不重复
- 更容易维护

### recalculateCourseStudentCounts 的意义

学生的增删改会影响课程的选课人数。

项目没有每次实时 join 统计，而是把 `courses.student_count` 作为冗余字段维护。

好处：

- 课程列表查询快
- 工作台图表查询快

缺点：

- 写操作后需要同步更新冗余数据

这是典型的“用写复杂度换读性能”的设计思路。

---

## 6.8 工作台统计模块设计

工作台接口在 `server-nest/src/dashboard/`。

它返回两类数据：

### 1. stats

- totalCourses
- publishedCourses
- totalStudents
- activeStudents

### 2. charts

- enrollment
- activity
- statusDist
- categoryDist

### 设计亮点

后端直接把图表数据整理成前端可消费结构：

```json
{ "name": "...", "value": 123 }
```

而不是返回杂乱原始数据再让前端拼。

这样前端页面会更轻，职责更清晰。

### activity 的生成逻辑

后端按最近 7 天逐日回溯，日期通过 `formatLocalDate()` 按本地自然日格式化：

- 查询当天学习人数
- 查询当天学习总时长
- 转成图表数据

说明后端在这个模块中承担了统计层职责，而不是只做简单 CRUD。

---

## 6.9 学习总结模块设计

学习总结接口在 `server-nest/src/summary/`。

逻辑包括：

1. 当前用户自己的总结分页列表。
2. 总结详情、创建、编辑、删除。
3. 服务端按 `user_id` 限制访问，避免跨用户读取或删除。
4. 时间通过 `formatLocalDateTime()` 输出本地日期时间字符串。

这种设计体现了“服务端保存内容和访问边界，前端负责 Markdown 渲染”的分工。

---

## 6.10 静态资源模块设计

静态资源接口在 `server-nest/src/static/`。

用途：

- 给 Markdown 图片提供访问地址
- 支持 `server/data/assets` 下的图片资源

### 安全点 1：路径必须落在 DATA_ROOT 内

通过检查：

- 拼接后的路径
- 是否仍以 `DATA_ROOT` 开头

来防止：

- `../`
- 目录穿越

### 安全点 2：扩展名白名单

只允许：

- png
- jpg
- jpeg
- gif
- webp
- svg

这能避免把该接口变成任意文件下载入口。

---

## 7. 数据库设计

当前默认数据库为 MySQL，表结构由 `server-nest/src/database/migrations/1788134400000-create-initial-tables.ts` 管理，实体类位于 `server-nest/src/database/entities/`。

启动时的数据职责拆分如下：

- `typeorm.config.ts`：读取 MySQL 环境变量、创建 TypeORM 连接配置，并在连接前确保数据库存在。
- `migrations/1788134400000-create-initial-tables.ts`：创建业务表、权限表和必要外键；遇到旧库脏引用时跳过外键创建，避免阻断启动。
- `database.init.ts`：仅补齐系统默认角色、权限、角色权限映射和默认账号。
- `recent-learning-activity.service.ts`：在数据库暂无真实行为表时，按当前逻辑每日补齐近 7 天学习活跃度。
- `date.util.ts`：统一本地日期和本地日期时间格式，避免 UTC 日期截断或固定时区偏移。

## 7.1 users 表

字段：

- id
- username
- password
- name
- role
- avatar
- created_at

作用：

- 存储登录用户

---

## 7.2 refresh_tokens 表

该表是服务端会话存储，不保存 Refresh Token 明文。核心字段包括：

- `token_hash`：随机 Refresh Token 的 SHA-256 哈希，唯一索引。
- `user_id`：会话所属用户。
- `family_id`：同一次登录及其轮换 Token 的会话族标识。
- `status`、`revoked_at`：会话是否已撤销。
- `idle_expires_at`、`absolute_expires_at`：分别控制 24 小时闲置期和 7 天最大会话期。

Refresh Token 轮换、退出当前设备、退出全部设备、改密码后的全会话失效，都通过该表完成；Access Token 中保存 `sessionId`，Guard 以此查询会话有效性。

## 7.3 courses 表

字段：

- id
- name
- description
- instructor
- cover
- category
- status
- student_count
- lesson_count
- created_at
- updated_at

关键点：

- `student_count` 是冗余统计字段
- `status` 表示课程发布状态

---

## 7.4 students 表

字段：

- id
- name
- student_no
- class_name
- phone
- email
- status
- course_ids
- created_at
- updated_at

关键点：

- `student_no` 唯一
- `course_ids` 是 JSON 数组字符串

---

## 7.5 learning_records 表

字段：

- id
- student_id
- course_id
- date
- duration

作用：

- 支撑工作台近 7 天学习活跃图表

---

## 7.6 roles 表

字段：

- code
- name
- description
- editable
- builtin
- deletable
- updated_at
- created_at

作用：

- 保存角色信息
- 区分系统内置角色和自定义角色
- 控制角色是否可编辑、是否可删除

关键点：

- `admin` 是不可变角色，`editable = 0`、`deletable = 0`
- 自定义角色由后端生成 `custom_xxx` 格式的稳定 code
- 删除角色前必须确认没有用户绑定该角色

---

## 7.7 permissions 表

字段：

- code
- name
- module
- module_name
- sort_order

作用：

- 保存系统支持的权限字典
- 为权限管理页提供按模块分组展示的数据
- 防止前端提交不存在的权限码

权限码采用 `module:action` 格式，例如：

```text
courses:view
courses:update
accounts:updateRole
```

---

## 7.8 role_permissions 表

字段：

- role_code
- permission_code
- created_at

作用：

- 保存角色与权限的多对多关系
- 支持管理员在页面中动态配置角色权限

关键点：

- 使用 `(role_code, permission_code)` 作为联合主键，避免重复授权
- 外键关联 `roles(code)` 和 `permissions(code)`
- 角色删除时通过外键级联清理对应权限关系

---

## 7.9 为什么使用 MySQL + TypeORM

当前默认后端已经从 Koa + SQLite 演进为 NestJS + MySQL + TypeORM，主要原因是：

- MySQL 更接近真实中后台项目的部署环境。
- TypeORM 提供实体类、Repository、QueryBuilder 和 migration，数据层边界更清晰。
- NestJS 与 TypeORM 集成度高，方便用依赖注入管理各模块 Repository。
- migration 能把表结构变更纳入版本管理，避免手动建表不可追踪。
- `DatabaseInit` 只负责默认系统数据；近 7 天学习活跃度由数据层独立服务每日补齐，避免把建表、系统初始化和统计逻辑混在一起。

### 面试可能追问

“既然用了 TypeORM，为什么还保留 mysql2？”

可以回答：

- TypeORM 的 MySQL 驱动底层依赖 `mysql2`，所以 `mysql2` 仍然是必要运行依赖。
- 业务代码不再直接使用 `mysql2/promise` 拼 SQL，而是通过 Repository 和 QueryBuilder 访问数据库。
- 只有 `typeorm.config.ts` 在连接前使用 `mysql2` 创建数据库，属于基础设施逻辑，不是业务数据访问层。

旧版 `server` 目录仍保留 SQLite 版本，它的价值是：

- 作为迁移前后的行为对照。
- 必要时临时回退。
- 帮助理解从轻量原型到结构化后端的演进过程。

---

## 8. 接口设计风格

项目接口风格整体较统一：

- 资源导向清晰
- HTTP 方法语义明确
- JSON 结构统一
- 查询参数命名规范

例如：

- `GET /api/courses`
- `POST /api/courses`
- `PUT /api/courses/:id`
- `DELETE /api/courses/:id`
- `PATCH /api/courses/:id/status`

这种设计在面试里通常会被认为是比较规范的 REST 风格。

---

## 9. 请求封装与错误处理

前端与默认 NestJS 后端的完整接口协议见：

```text
server-nest/docs/Client 与 server-nest 接口协议文档.md
```

日常新增或修改接口时，优先更新这份协议文档，再同步检查 `client/src/api.ts`、`client/src/types.ts`、Nest Controller/DTO 和后端契约测试。

## 9.1 request.ts 的职责

前端所有请求都经过 `client/src/utils/request.ts`。

它做了三件关键事情：

1. 自动注入 token
2. 统一判断 `ApiEnvelope.code`
3. 统一抛出 `Error`

### 为什么统一抛 Error

这样 store 或页面层可以统一：

- `try/catch`
- 用 `appErrorMessage()` 处理
- 再把错误交给全局错误提示或字段错误提示

---

## 9.2 全局错误处理策略

大多数业务错误最终进入 `auth-store.globalError`。

由布局层 `AppShell` 统一显示 `Alert`。

好处：

- 页面少写重复错误 UI
- 交互一致
- 用户更容易感知系统状态

当然，字段级错误仍然保留在表单里，比如：

- 学号已存在

这种错误既可以显示在全局，也可以回填到具体字段上。

---

## 10. 前后端数据流详解

这里以几个典型场景说明整个系统如何工作。

## 10.1 登录流程

```text
用户输入账号密码
  -> LoginPage 提交
  -> auth-store.handleLogin()
  -> api.login()
  -> request.ts 发送 POST /api/auth/login
  -> NestJS AuthController / AuthService 登录校验
  -> getEffectivePermissions(user) 计算权限集合
  -> 创建 MySQL Refresh Token 会话并写入 HttpOnly Cookie
  -> 返回 15 分钟 Access Token + user.permissions
  -> auth-store 写入内存 token/user/permissions
  -> 菜单、路由、按钮读取权限状态
  -> 跳转到第一个可访问页面
```

---

## 10.2 冷启动恢复登录态

```text
刷新页面
  -> AppRouterProvider 调用 initializeAuth()
  -> POST /api/auth/refresh，浏览器自动携带 HttpOnly Cookie
  -> 服务端校验并轮换 MySQL Refresh Token 会话
  -> 返回新的 Access Token、当前用户和权限集合
  -> 写回 auth-store
  -> 菜单、路由、按钮恢复到最新权限状态
```

---

## 10.3 课程列表查询

```text
用户选择筛选条件
  -> 页面调用 updateQuery()
  -> course-store.refreshList()
  -> api.fetchCourses(query)
  -> GET /api/courses
  -> 后端用 TypeORM QueryBuilder 拼查询条件和排序
  -> MySQL 返回分页数据
  -> store 更新 data
  -> Table 重新渲染
```

---

## 10.4 角色权限配置流程

```text
管理员进入权限管理页
  -> 并行请求 /api/system/roles 和 /api/system/permissions
  -> 页面展示角色列表和按模块分组的权限复选框
  -> 管理员勾选权限
  -> 前端自动补齐写操作依赖的 view 权限
  -> PATCH /api/system/roles/:code/permissions
  -> 后端校验 JWT 和 accounts:updateRole 权限
  -> 后端确认当前用户是 admin
  -> validatePermissions() 校验权限码合法性
  -> normalizePermissions() 再次补齐依赖权限
  -> 事务替换 role_permissions
  -> 返回更新后的角色
  -> 页面刷新权限数量和勾选状态
```

---

## 10.5 学生新增流程

```text
用户填写表单
  -> antd Form 同步校验
  -> 学号失焦异步唯一性校验
  -> 点击保存
  -> student-store.submitForm()
  -> POST /api/students
  -> 后端 parseStudentPayload 校验
  -> 写入 students
  -> updateCourseCounts()
  -> 返回新学生
  -> 刷新列表
```

---

## 11. 面试官可能会问的问题

下面这部分可以直接当面试备战提纲。

## 11.1 为什么选择 Zustand 而不是 Redux

可回答：

- 这个项目状态规模中等，不需要 Redux 那么重
- Zustand API 更轻量，样板代码少
- 对 CRUD 管理后台非常适合
- 学习和维护成本更低

但也可以补充：

- 如果项目再复杂一些，涉及时间旅行调试、复杂中间件、团队规范，也可以考虑 Redux Toolkit

---

## 11.2 为什么前端要做表单校验，后端还要再校验一次

可回答：

- 前端校验是为了用户体验
- 后端校验是为了数据安全和最终可信性
- 前端永远不能替代后端校验

---

## 11.3 为什么使用双 Token，而不是只用 JWT

可回答：

- Access Token 保持 JWT 的轻量请求鉴权优势，且只保留 15 分钟。
- Refresh Token 会话落在 MySQL，使退出、改密和风险会话能够立即撤销。
- Refresh Token 通过 HttpOnly Cookie 传输，不暴露给 JavaScript；数据库仅存哈希。
- 轮换与会话族撤销可以识别旧 Token 重放，弥补单 JWT 到期前难以撤销的问题。

---

## 11.4 你的权限系统为什么不只在前端控制

可回答：

- 前端权限控制是体验层，负责隐藏菜单、拦截路由、隐藏按钮
- 后端接口鉴权才是安全层，负责防止用户绕过前端直接调用接口
- 本项目所有核心接口都接入 `JwtAuthGuard + PermissionsGuard`
- `PermissionsGuard` 会重新查询数据库中的最新用户角色，避免旧 token 携带过期角色导致越权

可以总结成一句话：

“菜单、路由、按钮解决用户入口和交互体验，接口鉴权解决真正的安全边界。”

---

## 11.5 如果课程和学生的关系更复杂，你会怎么改数据库

可回答：

会把 `course_ids` JSON 数组改成中间表，例如：

- `student_courses(student_id, course_id)`

优点：

- 查询更规范
- 可 join
- 更适合统计
- 更容易维护关系数据

---

## 11.6 为什么要维护 courses.student_count 冗余字段

可回答：

- 因为课程列表和工作台都频繁展示选课人数
- 每次动态聚合会增加查询复杂度
- 这个项目写操作不频繁，适合“写时更新冗余字段”

同时也要说明风险：

- 如果更新漏掉，会产生数据不一致
- 更成熟系统可能用数据库触发器、事件驱动或定时校准

---

## 11.7 为什么课程要同时保存分类外键和分类名称

可回答：

- `courses.category_id` 用来关联 `course_categories.id`，保证正常业务操作时分类来源可靠
- `courses.category` 是分类名称快照，分类被数据库层直接删除或外键置空时仍能兜底展示
- 业务层不允许删除已有课程的分类，数据库 `ON DELETE SET NULL` 是兜底保护，不是主要业务规则
- 修改分类名称时，后端会同步更新已关联课程的名称快照，避免外键和展示文本长期不一致

这是一种“关系约束 + 展示快照”的折中设计，适合当前后台系统这种读展示多、分类变动少的场景。

---

## 11.8 为什么图表数据由后端返回而不是前端计算

可回答：

- 后端更靠近数据源
- 聚合成本更适合放在服务端
- 前端只负责展示，职责更清晰
- 减少前端拿原始数据后的二次加工逻辑

---

## 11.9 路由守卫为什么放在布局层

可回答：

- 集中处理登录校验
- 所有受保护页面共享一套逻辑
- 便于统一布局、导航和错误提示
- 降低页面重复代码

---

## 11.10 如何避免重复初始化请求

可回答：

- 通过 `initializePromise` 共享一次初始化过程
- 多个调用方拿到同一个 Promise
- 这样不会并发请求 `/auth/me`

这是一个很容易加分的细节。

---

## 11.11 这个项目有哪些可以继续优化的地方

可以从以下方向展开：

### 1. 数据模型优化

- 把 `course_ids` 改成中间表

### 2. 权限系统

- 支持一个账号绑定多个角色
- 权限变更后通过事件、轮询或 WebSocket 主动刷新在线用户权限
- 将前后端权限码抽成 shared 包，减少双端手动同步成本
- 增加更细粒度的数据权限，例如“只能维护自己创建的学习总结”

### 3. 错误处理

- 增加更细粒度的错误码
- 加入统一日志与监控

### 4. 前端体验

- 把查询参数同步到 URL
- 增加防抖搜索
- 优化表单复用

### 5. 安全性

- JWT 刷新机制
- 密码复杂度校验
- 登录失败次数限制

### 6. 工程化

- 增加单元测试和集成测试
- 增加 ESLint/Prettier 严格规范
- 引入 CI/CD

---

## 12. 这个项目最值得向面试官强调的点

如果你要在面试里讲这个项目，我建议强调下面这些点：

### 1. 这是完整闭环项目

不是只写页面，也不是只写接口，而是：

- 前端页面
- 状态管理
- 路由守卫
- 后端接口
- 数据库存储
- 图表统计
- 部署托管

全部打通。

### 2. 有工程化思维

比如：

- 统一请求封装
- 统一响应结构
- store 分层
- 全局错误处理
- 登录态恢复
- store 重置机制

### 3. 有技术亮点

比如：

- 自写轻量 Markdown 渲染器
- 图表容器封装
- 冗余统计字段维护
- 课程/学生列表查询条件统一管理

### 4. 有清晰的取舍意识

比如：

- 为什么从 SQLite 演进到 MySQL + TypeORM
- 为什么用 Zustand 而不是 Redux
- 为什么 `course_ids` 初期直接用 JSON
- 为什么统计先放服务端做

面试官通常不要求你一上来就用最复杂方案，更看重你是否知道方案的边界和演进方向。

---

## 13. 本项目的学习建议

如果你要系统学习这个项目，建议按这个顺序：

### 第一遍：看主流程

- 前端入口
- 路由守卫
- 登录流程
- 后端认证路由

### 第二遍：看 CRUD 模块

- 课程管理
- 学生管理
- store 和页面如何配合

### 第三遍：看亮点模块

- 工作台图表
- Markdown 渲染器
- 静态资源路由

### 第四遍：看设计取舍

- 为什么状态这样拆
- 为什么数据库这样建
- 为什么错误处理这样做

---

## 14. 启动与调试

## 14.1 前端

```bash
cd client
npm install
npm run dev
```

默认开发地址：

- `http://localhost:5173`

---

## 14.2 后端

```bash
cd server-nest
npm install
npm run dev
```

默认地址：

- `http://localhost:3000`

---

## 14.3 联调关系

前端开发环境下，Vite 代理：

- `/api` -> `http://localhost:3000`

因此页面请求不需要写完整后端地址，只要请求 `/api/...` 即可。

---

## 14.4 Docker 演示与生产配置

根目录 `docker-compose.yml` 使用 Nginx 托管前端并反向代理 `/api` 到 NestJS 服务，MySQL 仍连接宿主机 `host.docker.internal`。启动命令：

```bash
docker compose up --build -d
```

本地通过 `http://localhost` 访问。`server-nest/.env` 提供 MySQL、`JWT_SECRET` 和 `COOKIE_SECURE` 配置；Compose 使用 `env_file` 传递该文件，避免默认 JWT 密钥覆盖本地密钥。

- 本地 HTTP：`COOKIE_SECURE=false`，否则浏览器不会保存 Secure Cookie。
- 正式 HTTPS：使用强随机 `JWT_SECRET`，并设置 `COOKIE_SECURE=true`。
- `TYPEORM_MIGRATIONS_RUN=true` 会在启动时应用包括 `refresh_tokens` 在内的迁移；生产环境应改为显式迁移流程。

---

## 15. 总结

这个项目的价值在于，它虽然规模不大，但完整覆盖了一个中后台系统的核心技术链路：

- React 页面组织
- Zustand 状态管理
- 路由守卫与登录态恢复
- Axios 请求封装
- NestJS 接口设计
- MySQL + TypeORM 数据建模
- 统计聚合
- 图表展示
- Markdown 内容渲染
- 静态托管与前端回退

如果从“学习项目”的角度看，它足够全面。

如果从“面试项目”的角度看，它也足够有内容，因为你可以围绕以下主线展开：

- 架构怎么拆
- 数据怎么流
- 状态怎么管
- 鉴权怎么做
- 统计怎么做
- 为什么这么设计
- 后续怎么演进

真正能打动面试官的，不只是你写了什么功能，而是你是否能把这些功能背后的设计理由讲清楚。

---

## 16. 进阶技术细节拆解

这一章重点不是重复介绍模块，而是把项目中容易被忽略、但在面试里很容易被继续追问的技术细节单独拎出来讲。

## 16.1 为什么前端 API 要分成 `api.ts` 和 `request.ts`

很多初学者会把所有请求都直接写在页面里，例如：

```ts
useEffect(() => {
  axios.get('/api/courses').then(...)
}, [])
```

这种写法在 demo 阶段没问题，但一旦页面增多，问题会很快暴露：

- token 注入逻辑会到处重复
- 错误处理不统一
- 请求类型不清晰
- 页面组件过于臃肿

本项目把请求层拆成两层：

### 第一层：`request.ts`

职责是“基础设施层”：

- 创建 Axios 实例
- 设置 `baseURL`
- 设置超时
- 注入 token
- 统一解析响应格式
- 统一处理 401 和错误消息

### 第二层：`api.ts`

职责是“业务接口层”：

- 声明接口名称
- 管理 URL
- 管理查询参数序列化
- 对返回值做类型约束

这种拆法的好处是：

- 页面只关心“我要什么数据”
- 不关心“这个请求怎么鉴权、怎么解包、怎么处理错误”

从架构角度讲，这相当于把“通信协议细节”从业务层抽离了出去。

---

## 16.2 为什么全局错误放在 `auth-store` 里

从语义上看，把 `globalError` 放在 `auth-store` 里好像有点奇怪，因为很多错误并不属于认证模块。

但从项目演进角度看，这是一种“工程上够用且集中”的方案。

原因如下：

### 1. 登录态是整个应用的最顶层状态

布局、导航、路由守卫都围绕登录态组织，因此把全局错误也放在一个顶层 store 里，可以减少跨 store 通信复杂度。

### 2. 布局层天然依赖 auth-store

`AppShell` 已经依赖用户信息和登出行为，所以直接读取 `globalError` 最方便。

### 3. 错误展示位置本身也是全局性的

既然错误被展示在布局顶部，而不是某个独立业务页局部区域，那么它从某种意义上就是一个“全局 UI 状态”。

### 这种做法的边界

如果项目继续变大，更推荐把它抽成：

- `app-store`
- `ui-store`
- `notification-store`

这样语义更清晰。

所以面试时可以回答：

- 当前项目规模下放在 `auth-store` 是一种轻量实现
- 更大规模系统会考虑单独的全局 UI 状态仓库

---

## 16.3 为什么使用 `useShallow`

课程页和学生页在读取 Zustand store 时使用了：

```ts
useShallow(...)
```

它的主要作用是减少不必要的重新渲染。

### 背景

如果组件直接订阅一个对象：

```ts
const state = useStore((s) => ({
  a: s.a,
  b: s.b,
}));
```

每次 store 更新都会返回一个新对象引用，即使 `a` 和 `b` 没变，也可能触发组件重新渲染。

### `useShallow` 的作用

它会做浅比较：

- 如果字段值没变，就不触发重渲染
- 如果字段确实变了，才更新页面

### 这在本项目中的价值

列表页状态很多：

- loading
- data
- formOpen
- editingId
- draftKeyword
- categories/classes/courses

如果不做选择性订阅，性能虽然不至于崩，但会产生很多无意义重渲染。

---

## 16.4 为什么表单弹窗使用 `destroyOnHidden`

课程页和学生页的弹窗都配置了：

- `destroyOnHidden`

它的价值在于：关闭弹窗后销毁内部内容。

### 好处

- 下次打开重新走表单初始逻辑
- 避免隐藏状态下保留旧输入
- 避免部分受控/非受控组件状态错乱

### 风险

如果弹窗里有特别复杂的内容，每次重新挂载可能有性能成本。

但在本项目里，表单规模适中，这个取舍是合理的。

---

## 16.5 为什么课程和学生列表不使用 Table 自带分页

项目选择关闭 Table 内建分页，而使用自己的 `PaginationBar`。

### 原因

#### 1. 统一所有页面的分页交互

课程页、学生页都使用相同的分页行为：

- 页码切换
- pageSize 切换
- 总条数展示

统一成一个组件更利于维护。

#### 2. 逻辑收敛在 store 的 `query`

分页本质上是查询参数的一部分，不应该由表格组件自己“悄悄管理”。

#### 3. UI 样式统一

通过自定义分页栏，可以让视觉和交互跟整个后台一致。

### 面试答法

如果被问“为什么不用现成分页”，可以说：

- 不是不能用，而是为了更统一地控制查询参数和 UI 行为
- 本项目希望把分页作为查询状态的一部分显式管理

---

## 16.6 表单校验的层次设计

学生表单是这个项目中校验最完整的一个地方。

它有三层校验：

### 第一层：基础同步校验

例如：

- 必填
- 最大长度
- 正则格式

这类校验特点：

- 快
- 无需请求后端
- 能第一时间给用户反馈

### 第二层：异步业务校验

例如：

- 学号唯一性

它必须请求服务端，因为“当前学号是否存在”是服务端数据问题，不是纯前端规则问题。

### 第三层：提交后的服务端最终校验

即使前端已经校验过，后端仍然必须再检查：

- 防止绕过前端
- 防止并发提交导致的数据竞争
- 保证数据一致性

### 为什么这是典型面试点

因为它能体现你是否理解：

- 用户体验
- 数据安全
- 前后端职责边界

---

## 16.7 为什么 `checkStudentNoUnique()` 当前是通过列表接口实现的

前端的 `checkStudentNoUnique()` 实际上是：

- 走 `fetchStudents()`
- 再在结果里本地查重

而不是直接请求一个独立唯一性检查接口。

### 这是一种什么思路

这是“复用已有列表接口”的轻量做法。

优点：

- 少写一个接口也能完成功能
- 项目初期开发更快

缺点：

- 不是最精准的接口语义
- 请求数据可能比实际所需更多
- 当学生数量变大时效率较低

### 但注意

后端其实已经补了 `/students/check-no` 的能力，只是前端暂时没切过去。

这正是一个很好的“演进点”：

- 当前实现能用
- 下一步可以改成专门校验接口

这类“知道当前方案不完美，并清楚下一步怎么演进”的表达，面试里很加分。

---

## 16.8 为什么 `course_ids` 用 JSON 数组而不是关联表

这个问题非常适合面试，因为它涉及设计权衡。

### 当前做法

在 `students` 表中直接存：

```json
[1, 3, 5]
```

### 这样做的优点

- 实现简单
- 开发快
- 很适合作业项目或小型原型
- 后端写入逻辑直观

### 缺点

- 不能高效用 SQL 做关系查询
- 不能直接做 join
- 按课程过滤时需要在内存中处理
- 数据约束弱于中间表

### 如果升级到生产项目

推荐改成：

```text
students
courses
student_courses
```

中间表的优势：

- 关系表达更标准
- 查询更灵活
- 更适合统计和扩展
- 数据一致性更好

### 面试回答模板

“当前项目规模较小，为了降低建模复杂度和提高开发效率，我先用 JSON 数组存课程关系。这个方案的缺点是查询关系不够优雅，尤其是按课程筛选时效率不高。如果进入更真实的业务场景，我会拆成中间表 `student_courses`，把关系查询和约束交给数据库层处理。”

---

## 16.9 为什么 Koa 的静态托管要做 SPA 回退

服务端在处理非 `/api` 的 GET 请求时：

- 先看请求的资源文件是否存在
- 如果存在，直接返回静态文件
- 如果不存在，回退到 `index.html`

### 为什么必须这样做

因为前端使用的是浏览器路由：

- `/dashboard`
- `/courses`
- `/students`

用户如果直接刷新 `/courses`：

- 浏览器会把它当成一个真实 URL 去请求服务器
- 服务器如果不回退到 `index.html`，就会返回 404

### 面试常见问法

“为什么 React Router 项目部署后刷新页面会 404？”

答案就是：

- 浏览器刷新时请求的是服务端 URL
- 而前端路由只有在 `index.html` 启动后才能接管
- 所以服务端必须把未知前端路由回退到 `index.html`

---

## 16.10 为什么要对静态资源做目录穿越防御

在 `static.js` 中，路径经过：

- `join`
- `normalize`
- 再校验是否仍然以 `DATA_ROOT` 开头

这是为了防止类似这样的访问：

```text
/api/static/../../src/index.js
```

如果不做校验，攻击者可能读到：

- 服务器源码
- 配置文件
- 数据文件

### 这类问题在面试里属于什么层次

属于“你有没有安全意识”的问题。

哪怕项目本身不是高安全系统，能想到目录穿越防御，说明你不是只会堆功能。

---

## 17. 工程化与开发体验

## 17.1 Vite 配置

前端构建配置在 `client/vite.config.ts`。

当前配置要点：

- React 插件
- Tailwind Vite 插件
- 开发服务器监听 `0.0.0.0`
- 开发端口 `5173`
- 代理 `/api` 到 `3000`
- 预览端口 `4173`

### 为什么代理 `/api`

原因有两个：

#### 1. 避免跨域问题

前端开发时页面来自 `5173`，后端来自 `3000`，如果不代理，浏览器会触发跨域限制。

#### 2. 保持前后端 URL 写法统一

无论开发还是演示：

- 前端都写 `/api/...`
- 不需要写死后端完整地址

这让部署切换更平滑。

---

## 17.2 ESLint 配置

前端 lint 配置在 `client/eslint.config.js`。

启用了：

- JS 推荐规则
- TypeScript 推荐规则
- React Hooks 推荐规则
- React Refresh Vite 规则

### 这说明了什么

项目虽然不大，但已经具备一定工程规范意识：

- 不是纯手写而无质量约束
- 至少能约束 Hooks 使用
- 能约束 TS/JS 的常见问题

### 还能继续提升什么

- 增加 prettier
- 增加 import 排序
- 增加更严格的 TypeScript 规则
- 增加测试规则

---

## 17.3 测试现状与补测方向

项目目前已经补充了 NestJS 后端测试，重点覆盖 API 契约、数据库初始化、migration 和工具函数：

```text
server-nest/test/api-contract.e2e-spec.ts
server-nest/test/auth-session.e2e-spec.ts
server-nest/test/database-init.e2e-spec.ts
server-nest/test/migration.e2e-spec.ts
server-nest/test/typeorm-config.e2e-spec.ts
server-nest/test/date-util.e2e-spec.ts
```

已经覆盖的重点包括：

- 写操作权限自动补齐查看权限
- `admin` 始终拥有全部权限且权限不可修改
- 自定义角色创建、重命名、删除
- 仍有用户绑定的自定义角色不可删除
- 默认角色、权限、角色权限和默认账号会按主键补齐，不覆盖已有数据
- 多实例首次启动时重复键错误可安全忽略
- 近 7 天学习活跃度按本地自然日补齐
- migration 遇到旧库脏引用时跳过外键创建，避免阻断启动
- 非法路由 ID 返回稳定 404，不暴露底层数据库错误
- Access Token 15 分钟有效期、Refresh Cookie 属性、Token 轮换与旧 Token 重放检测
- 当前会话退出后，关联 Access Token 会立即失效

但前端测试还没有完全补齐。

### 如果继续完善，前端优先测试

- `pageAfterDelete()` 分页回退逻辑
- `appErrorMessage()` 错误格式化
- `parseMarkdown()` 对标题、列表、代码块的解析

### 如果继续完善，后端优先测试

- `parseStudentPayload()` 各种边界输入
- `/auth/login` 正确/错误密码
- `/students` 的学号唯一性校验
- `/static` 的目录穿越防护

这样会显得你有测试意识，只是项目尚未完整落地。

---

## 18. 性能、可维护性与扩展性分析

## 18.1 当前性能表现在哪些地方已经足够

对这个项目而言，当前性能在这些方面是足够的：

- 列表数据量小
- MySQL 连接池和 TypeORM Repository 能支撑当前数据量
- Zustand 状态量不大
- 图表数据量非常有限
- Markdown 内容较短

所以很多实现虽然不是“最强架构”，但在这个场景里完全合理。

这是面试里很重要的一种思维：

- 设计要匹配场景
- 不要过度设计

---

## 18.2 当前性能瓶颈会出现在哪里

如果数据量扩大，优先出现问题的点通常是：

### 1. `course_ids` JSON 数组查询

按课程筛选学生时需要先取出数据再内存过滤。

### 2. `updateCourseCounts()`

每次学生增删改都要遍历所有课程和所有学生，数据量大时写操作会慢。

### 3. Markdown 解析器

如果内容特别长，自写解析器会在前端做更多字符串处理。

### 4. 学号唯一性前端预检查

如果继续走列表接口查重，效率会越来越低。

---

## 18.3 如果要做性能优化，可以怎么做

### 后端

- 用中间表替代 JSON 数组关系
- 用 SQL 聚合替代内存遍历
- 对热点查询增加索引
- 使用更适合多用户场景的数据库

### 前端

- 为搜索增加防抖
- 将 query 同步到 URL，便于恢复页面状态
- 长列表使用虚拟滚动
- Markdown 解析结果可缓存

---

## 18.4 维护性分析

这个项目维护性较好的地方：

- 前后端职责比较清晰
- store 分模块拆分
- API 封装统一
- 文件结构规范
- 页面风格统一
- 现在又补上了详细注释

维护性一般的地方：

- 部分逻辑仍偏作业式直写，没有进一步抽象
- 学生和课程页面有部分重复代码
- Markdown 解析器是自写实现，未来功能扩展成本较高

### 如果继续重构

可以进一步抽象出：

- 列表页通用查询栏组件
- 通用 CRUD 弹窗表单模式
- 通用确认删除 Hook
- 通用 query 管理 Hook

---

## 19. 安全性分析

## 19.1 已有的安全措施

当前项目已经体现出一些基础安全意识：

- 密码使用 `bcrypt` 加密存储
- 登录失败不暴露用户名是否存在
- Access Token 仅存内存且 15 分钟过期
- Refresh Token 使用 HttpOnly、SameSite Cookie，数据库只保存哈希
- MySQL 会话支持 24 小时闲置过期、7 天最大会话期、令牌轮换和重放检测
- 401 通过单例刷新重试统一处理，刷新失败才清理登录态
- 后端通过 `JwtAuthGuard + PermissionsGuard` 做接口级权限校验
- Guard 校验 JWT 关联会话是否有效，退出或改密后旧 Access Token 立即失效
- 权限 Guard 按用户 ID 重新查库获取最新角色，避免旧 token 越权
- `admin` 角色不可编辑、不可删除，避免系统无人可管理
- 自定义角色删除前校验是否仍有用户绑定
- 静态资源接口有目录穿越防御
- 静态资源接口有文件类型白名单

---

## 19.2 仍然存在的安全边界

### 1. 生产 Cookie 与密钥配置

`JWT_SECRET` 已通过本地 `.env` 注入，Docker 不再用默认值覆盖它。生产环境需要使用独立密钥管理，并在 HTTPS 下设置 `COOKIE_SECURE=true`；本地 HTTP 开发环境才使用 `false`。

### 2. 没有登录失败限流

如果是生产系统，应该考虑：

- 登录失败次数限制
- IP 限流
- 验证码

### 3. Markdown 渲染边界有限

当前自写渲染器功能有限，如果以后支持更复杂 HTML/Markdown，需要更加重视 XSS 风险。

---

## 20. 常见面试追问与参考回答

这一章给出更接近真实面试表达的回答模板。

## 20.1 你在这个项目中最核心的设计是什么

参考回答：

“我在这个项目里最核心的设计是把中后台的通用能力拆清楚了。前端用 React Router 做路由守卫，用 Zustand 管理登录态和各模块查询状态，用 Axios 做统一请求拦截和错误处理。后端用 NestJS 提供 REST 风格接口，用 TypeORM 管理 MySQL 数据访问和 migration，并在工作台场景下由服务端直接做统计聚合。这样页面、状态、接口、数据库是成体系打通的，而不是单点功能堆积。”

如果要突出最大亮点，可以回答：

“我最核心的设计是动态 RBAC 权限体系。我把后台能力拆成 `module:action` 权限码，用 `roles`、`permissions`、`role_permissions` 表维护角色权限映射；登录和 `/auth/me` 动态下发权限集合；前端基于权限控制菜单、路由和按钮；后端通过 `JwtAuthGuard` 和 `PermissionsGuard` 做接口级鉴权。这样权限不是写死在页面里，而是形成了从数据库到前端展示再到接口安全的完整闭环。”

---

## 20.2 这个项目里你做过哪些工程化处理

参考回答：

“我做了几层工程化收口。第一层是请求封装，把 token 注入、统一响应解析和 401 处理放在 Axios 实例里。第二层是状态收口，课程页和学生页都用统一 query 管理分页、筛选和搜索。第三层是布局收口，把错误提示、导航和登录态校验集中在受保护布局层。第四层是服务端统一响应格式，这让前端不需要为不同接口写不同的成功失败解析逻辑。”

---

## 20.3 你为什么说这个项目不是简单 CRUD

参考回答：

“它当然包含 CRUD，但不只是 CRUD。比如动态 RBAC 权限体系涉及权限码、角色权限表、权限下发、菜单/路由/按钮/接口四层控制；登录态恢复涉及路由守卫和 token 校验；工作台涉及后端统计聚合和图表数据结构设计；学习总结模块涉及 Markdown 解析和静态资源映射；学生模块还涉及关系字段建模和课程选课人数的冗余统计回写。所以它更像一个完整后台系统的缩小版。”

---

## 20.4 如果让你把这个项目继续做成真实系统，你先改什么

参考回答：

“我会先改三件事。第一，把学生和课程关系从 JSON 数组改成中间表，提升查询和统计能力。第二，继续增强权限体系，比如支持多角色绑定、在线权限刷新和更细的数据权限。第三，在现有双 Token 会话机制上增加登录失败限流、审计日志和生产密钥管理，并继续补齐前端关键流程测试。这三件事能最快提升系统的可靠性、可维护性和可演进性。”

---

## 20.5 这个项目里哪部分最能体现你的思考

参考回答：

“我觉得最能体现思考的是动态 RBAC 权限体系和几个工程取舍。权限上我没有只做前端隐藏按钮，而是把权限码、角色权限落库、权限下发、前端门控和后端接口鉴权串起来；取舍上，项目规模不大，所以用 Zustand 代替 Redux，关系字段先用 JSON 数组快速落地，但我清楚它的边界和后续演进方向。这些都是在当前规模下追求成本、清晰度和可扩展性的平衡。”

---

## 21. 建议你如何讲述这个项目

如果你以后要在面试中讲项目，不建议从“我做了登录页、课程页、学生页”这种流水账开始。

更好的讲法是：

### 第一步：先用一句话定义项目

“这是一个前后端分离的在线学习管理后台，覆盖了登录鉴权、动态 RBAC 权限管理、列表管理、图表统计和 Markdown 内容渲染。”

### 第二步：讲系统主干

“前端用 React + Zustand + React Router，后端用 NestJS + TypeORM + MySQL，前端通过统一请求封装调用后端 REST 接口，后端返回统一响应结构。”

### 第三步：挑 2 到 3 个亮点深讲

例如：

- 路由守卫和登录态恢复
- 动态 RBAC 权限体系
- 学生模块的关系字段处理和冗余统计回写
- Markdown 渲染器和静态资源映射

### 第四步：讲取舍和演进

“当前为了降低复杂度采用了 JSON 数组关系字段，但如果业务增长，会演进成中间表。”

这种讲法比功能列表更像真正有经验的工程师表达。

---

## 22. 文档阅读地图

如果你是第一次看这个仓库，又希望尽可能系统地学透，可以按下面顺序对照源码和本文档一起阅读。

### 阶段一：快速建立主流程认知

先看这些文件：

- [main.tsx](./client/src/main.tsx)
- [App.tsx](./client/src/App.tsx)
- [routes.tsx](./client/src/router/routes.tsx)
- [page-elements.tsx](./client/src/router/page-elements.tsx)
- [auth-store.ts](./client/src/stores/auth-store.ts)
- [main.ts](./server-nest/src/main.ts)
- [app.module.ts](./server-nest/src/app.module.ts)

目标：

- 看懂应用怎么启动
- 看懂路由怎么保护
- 看懂登录态怎么恢复

### 阶段二：掌握前端请求和状态层

再看：

- [request.ts](./client/src/utils/request.ts)
- [api.ts](./client/src/api.ts)
- [course-store.ts](./client/src/stores/course-store.ts)
- [student-store.ts](./client/src/stores/student-store.ts)
- [summary-store.ts](./client/src/stores/summary-store.ts)

目标：

- 看懂请求怎么统一封装
- 看懂 store 如何组织页面状态
- 看懂页面为什么不直接写 axios

### 阶段三：掌握页面交互层

再看：

- [CoursesPage.tsx](./client/src/pages/CoursesPage.tsx)
- [StudentsPage.tsx](./client/src/pages/StudentsPage.tsx)
- [DashboardPage.tsx](./client/src/pages/DashboardPage.tsx)
- [SummaryPage.tsx](./client/src/pages/SummaryPage.tsx)
- [AppShell.tsx](./client/src/layouts/AppShell.tsx)

目标：

- 看懂查询区、列表区、分页区、弹窗区如何配合
- 看懂布局层怎么承载导航和全局错误

### 阶段四：掌握后端与数据库

再看：

- [typeorm.config.ts](./server-nest/src/database/typeorm.config.ts)
- [database.init.ts](./server-nest/src/database/database.init.ts)
- [create-initial-tables.ts](./server-nest/src/database/migrations/1788134400000-create-initial-tables.ts)
- [entities](./server-nest/src/database/entities)
- [auth.service.ts](./server-nest/src/auth/auth.service.ts)
- [courses.service.ts](./server-nest/src/courses/courses.service.ts)
- [students.service.ts](./server-nest/src/students/students.service.ts)
- [dashboard.service.ts](./server-nest/src/dashboard/dashboard.service.ts)
- [summary.service.ts](./server-nest/src/summary/summary.service.ts)
- [static.service.ts](./server-nest/src/static/static.service.ts)

目标：

- 看懂数据表结构
- 看懂 CRUD 和统计怎么落地
- 看懂服务端安全边界

### 阶段五：掌握项目亮点

最后看：

- [权限管理设计文档.md](./权限管理设计文档.md)
- [permissions.constants.ts](./server-nest/src/permissions/permissions.constants.ts)
- [permission.service.ts](./server-nest/src/permissions/permission.service.ts)
- [RequirePermission.tsx](./client/src/router/RequirePermission.tsx)
- [Permission.tsx](./client/src/components/Permission.tsx)
- [PermissionsPage.tsx](./client/src/pages/PermissionsPage.tsx)
- [markdown.tsx](./client/src/markdown.tsx)
- [chart-core.tsx](./client/src/components/echarts/chart-core.tsx)
- [CourseCategoryChart.tsx](./client/src/components/echarts/CourseCategoryChart.tsx)
- [StudentStatusChart.tsx](./client/src/components/echarts/StudentStatusChart.tsx)

目标：

- 看懂动态 RBAC 权限体系的设计和执行链路
- 看懂自写 Markdown 渲染器的思路
- 看懂图表组件的封装方式
- 看懂为什么说这个项目除了 CRUD 之外还有亮点

---

## 23. 后端 DTO 与 ValidationPipe 改造

`server-nest` 已补充 DTO 和全局 `ValidationPipe`，后端入参处理从“Controller/Service 中临时解析对象”进一步收敛为：

```text
HTTP 请求
  -> Controller 接收 DTO
  -> ValidationPipe 做基础格式校验和类型转换
  -> Service 做业务规则和数据库校验
  -> TypeORM Repository / QueryBuilder 访问 MySQL
```

本次改造保持接口路径、字段名、响应结构和数据库结构不变。DTO 只处理基础字段形态，例如必填、枚举、手机号、邮箱、分页数字、权限数组；涉及数据库的数据规则仍留在 Service，例如学号重复、课程是否存在、角色是否可分配、管理员账号保护。

全局 `ValidationPipe` 使用 `transform: true`、`whitelist: true`、`forbidNonWhitelisted: false`。这样 query 参数可以按 DTO 转成数字，未声明字段会被剥离，但不会因为前端多传临时字段而直接 400。校验失败会通过 `ApiException` 返回旧格式：

```json
{ "code": 400, "msg": "课程状态不合法", "data": null }
```

路由 `id` 没有改成 `ParseIntPipe`，仍由现有 `parsePositiveIntId()` 处理，保证非法 ID 的 404 行为不变。

---

## 24. 结尾建议

这份文档最好的使用方式，不是一次性从头背到尾，而是：

- 第一次通读，建立整体认知
- 第二次对照源码逐章看
- 第三次重点准备“面试追问”和“回答模板”

如果你能把这份文档里的以下几条真正讲顺：

- 登录态恢复的完整链路
- 动态 RBAC 权限体系如何从角色权限表、权限下发走到前端控制和接口鉴权
- 查询状态为什么用统一 query 管理
- 为什么工作台统计放后端做
- 为什么 `course_ids` 当前这样设计，以及后续如何演进
- 为什么静态托管需要 SPA 回退和目录穿越防御

那这个项目在面试里就已经不是“一个普通作业”，而是一个你真正理解过、拆解过、能讲清楚的项目。
